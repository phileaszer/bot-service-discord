{
  "name": "sentinel",
  "version": "1.0.0",
  "description": "Bot Discord bilingue pour services, modération, dossiers et dashboard web.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node --use-system-ca index.js",
    "announce": "node --use-system-ca tools/announce-discord.js",
    "deploy:commands": "node --use-system-ca deploy-commands.js",
    "backup:db": "node --use-system-ca tools/backup-database.js",
    "restore:db": "node --use-system-ca tools/restore-database.js",
    "check": "node --check index.js && node --check dashboard.js && node --check site/auth.js && node --check site/dashboard.js && node --check site/i18n.js && node --check site/status.js && node --check site/theme.js && node --check deploy-commands.js && node --check database/database.js && node --check server-sync.js && node --check tools/announce-discord.js && node --check tools/backup-database.js && node --check tools/restore-database.js"
  },
  "keywords": [
    "discord",
    "sentinel",
    "moderation",
    "dashboard",
    "service",
    "tickets"
  ],
  "author": "",
  "license": "UNLICENSED",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  },
  "overrides": {
    "undici": "^6.28.0"
  }
}
